# Signal — environment variables
# Copy to .env.local for local dev (gitignored). Set the same in Vercel envs.

# Cala REST API key (SERVER-ONLY — never expose to the browser).
# Get one at https://console.cala.ai/api-keys
CALA_API_KEY=

# Optional overrides
# CALA_BASE_URL=https://api.cala.ai/v1
# ASK_DAILY_CEILING=20
